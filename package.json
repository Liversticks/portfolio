{
  "name": "liversticks-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @11ty/eleventy --input=. --formats=html,ejs,css",
    "clean": "rmdir _site /s /q && rm -rf ./_site",
    "serve": "npx @11ty/eleventy --serve"
  },
  "keywords": [],
  "author": "liversticks",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1"
  }
}
